import React from 'react';

function Header() {
    return (
        <header className="hero py-4">
            <div className="hero-body">
                <h1 className="mt-3 is-unselectable columns is-centered">The Brewery Locator</h1>
                <h2 className="mt-3 is-unselectable columns is-centered">THERE'S A COLD ONE JUST AROUND THE CORNER</h2>
            </div>
        </header>
    );
}

export default Header;
